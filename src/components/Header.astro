---
---

<style>
  header {
    margin: 0;
    padding: 0;
    background: white;
    box-shadow: 0 2px 8px rgba(var(--black), 5%);
    position: sticky;
    top: 0;
    z-index: 100;
    width: 100%;
    line-height: 0;
  }

  nav {
    width: 100%;
    overflow: hidden;
  }

  .nav-inner {
    width: 100%;
    max-width: 100%;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: clamp(1.5rem, 5vw, 4rem);
    flex-wrap: nowrap;
    line-height: normal;
    padding: 0.65rem 1.5rem 0.65rem 1rem;
  }

  .nav-links {
    display: flex;
    flex-wrap: wrap;
    gap: clamp(0.4rem, 1vw, 0.75rem);
    align-items: center;
  }

  .nav-links a {
    display: inline-flex;
    align-items: center;
    text-decoration: none;
    color: inherit;
    background: transparent;
    border: none;
    line-height: 1;
  }

  .nav-links a::before,
  .nav-links a::after {
    content: none;
    display: none;
  }

  .nav-button-wrapper {
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    height: 82px;
    min-width: 120px;
    padding: 0 8px;
    box-sizing: border-box;
    opacity: 1;
    transform: none;
  }

  .nav-button {
    display: block;
    width: 100%;
    height: 100%;
    max-height: 100px;
    position: absolute;
    inset: 0;
    object-fit: cover;
    z-index: 1;
    transform-origin: center;
    transform: scale(0.9);
  }

  .nav-gif {
    position: relative;
    z-index: 2;
    width: auto;
    height: 100%;
    max-height: 84px;
    display: block;
    transform: scale(0.95);
    transition: opacity 0.18s ease;
  }

  .nav-button-text {
    position: absolute;
    width: 100%;
    z-index: 10;
    font-family: "Overload Burn", sans-serif;
    font-weight: 900;
    font-size: 1.25rem;
    color: #fff;
    pointer-events: none;
    text-align: center;
    text-shadow: 0 3px 8px rgba(0,0,0,0.6);
  }

  .social-links {
    display: flex;
    gap: clamp(0.5rem, 1.5vw, 1rem);
    align-items: center;
    flex-shrink: 0;
    justify-content: center;
    margin-right: 1.5rem;
  }

  .social-links a { display: inline-flex; }

  .gif-wrapper {
    position: relative;
    width: clamp(52px, 6vw, 85px);
    height: clamp(48px, 6vw, 80px);
    overflow: hidden;
  }

  .gif {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: contain;
    transition: opacity 0.18s ease;
    backface-visibility: hidden;
    will-change: opacity;
    background-color: transparent;
  }

  .gif.base { opacity: 1; z-index: 1; }
  .gif.hover { opacity: 0; z-index: 2; }
  .gif-wrapper:hover .gif.hover { opacity: 1; }
  .gif-wrapper:hover .gif.base { opacity: 1; }

  /* ── HAMBURGER ── */
  .hamburger {
    display: none;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 44px;
    height: 44px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 6px;
    z-index: 200;
    flex-shrink: 0;
  }

  .hamburger span {
    display: block;
    width: 100%;
    height: 3px;
    background: #0099cc;
    border-radius: 2px;
    transition: transform 0.3s ease, opacity 0.3s ease;
  }

  .hamburger.is-open span:nth-child(1) {
    transform: translateY(8px) rotate(45deg);
  }
  .hamburger.is-open span:nth-child(2) {
    opacity: 0;
  }
  .hamburger.is-open span:nth-child(3) {
    transform: translateY(-8px) rotate(-45deg);
  }

.mobile-drawer {
  display: none;
  position: fixed;
  top: 0;
  right: 0;
  width: min(300px, 80vw);
  height: 100dvh;
  z-index: 150;
  flex-direction: column;
  padding: 5rem 1.5rem 2.5rem 2.5rem;
  transform: translateX(100%);
  transition: transform 0.35s cubic-bezier(0.4, 0, 0.2, 1);
  overflow: visible;  /* allow pseudo-element to bleed left */
  background: none;
}

.mobile-drawer::before {
  content: '';
  position: absolute;
  top: 11%;
  left: -160%;
  width: 450%;       /* much larger — becomes the height after rotation */
  height: 70%;
  background-image: url('/box animation slow1.webp');
  background-size: 100% 100%;
  background-repeat: no-repeat;
  z-index: 0;
  pointer-events: none;
  transform: rotate(90deg);
  transform-origin: center center;
  opacity: 0;
  transition: opacity 0.2s ease;
}
.mobile-drawer.is-open::before {
  opacity: 1;
  transition: opacity 0.2s ease 0.05s;
}

.drawer-content {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
  height: 100%;
}


/* Make sure content stays above bg */
.drawer-content {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
  height: 100%;
}

  .mobile-drawer.is-open {
    transform: translateX(0);
  }

  /* Backdrop */
  .drawer-backdrop {
  display: none;        /* or display: block inside the mobile media query */
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.45);
  z-index: 140;
  opacity: 0;
  transition: opacity 0.35s ease;
  backdrop-filter: blur(2px);
}

.drawer-backdrop.is-open {
  opacity: 1;
}

  /* All drawer content sits above the bg image */
  .drawer-content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    height: 100%;
    gap: 0;
  }

  .drawer-nav-links {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 1.6rem;
  flex: 1;
  padding-top: 1rem;
}

  .drawer-nav-links a {
    display: flex;
    text-decoration: none;
    color: inherit;
    width: 100%;
    justify-content: center;
    /* prevent layout shift during drawer slide */
    will-change: auto;
  }

  .drawer-nav-links .nav-button-wrapper {
    height: 72px;
    min-width: 0;
    width: 100%;
    max-width: 260px;
    animation: none; /* no animation inside drawer */
  }

  /* Social section */
  .drawer-social-section {
    border-top: 1px solid rgba(0, 153, 204, 0.3);
    padding-top: 1.25rem;
    margin-top: 0.5rem;
  }

  .drawer-social-title {
    font-family: "Superglue", sans-serif;
    font-size: 0.75rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #0e3947;
    text-align: center;
    margin-bottom: 0.85rem;
  }

  .drawer-social {
    display: flex;
    flex-wrap: nowrap; /* keep all on one line */
    justify-content: center;
    align-items: center;
    gap: 0.5rem;
  }

  .drawer-social a { display: inline-flex; flex-shrink: 0; }

  .drawer-social .gif-wrapper {
    width: 48px;
    height: 44px;
  }

  /* ── DESKTOP BREAKPOINTS ── */
  @media (max-width: 1024px) {
    .nav-button-wrapper { height: 78px; min-width: 108px; padding: 0 6px; }
    .nav-button { max-height: 80px; }
    .nav-gif { max-height: 72px; }
    .nav-button-text { font-size: 1.1rem; }
    .gif-wrapper { width: 62px; height: 54px; }
  }

  /* ── MOBILE ── */
  .nav-brand { display: none; }

  /* ── MOBILE DRAWER REDESIGN ── */
@media (max-width: 768px) {
  .nav-links { display: none; }
  .social-links { display: none; }

  header {
    background: transparent;
    box-shadow: none;
  }

  .nav-inner {
    justify-content: flex-end;
    padding: 0rem 1rem;
    gap: 0;
  }

  .nav-brand { display: none; }

  .hamburger {
    display: flex;
    gap: 4px;
    width: 28px;
    height: 28px;
    padding: 4px;
    position: fixed;
    top: 1rem;
    right: 1rem;
  }

  .hamburger span {
    height: 2px;
    background: #ffffff;
  }

  .hamburger.is-open span {
    background: #00ccff;
  }

  .hamburger.is-open span:nth-child(1) {
    transform: translateY(6px) rotate(45deg);
  }
  .hamburger.is-open span:nth-child(3) {
    transform: translateY(-6px) rotate(-45deg);
  }

 .mobile-drawer {
    display: flex;
    position: fixed;
    top: 0;
    right: 0;
    width: min(340px, 85vw);
    height: 100dvh;
    z-index: 150;
    flex-direction: column;
    padding: 0.1rem 1rem 2rem;
    transform: translateX(100%);
    transition: transform 0.35s cubic-bezier(0.4, 0, 0.2, 1);
    overflow: visible;
    background: none;
  }

  .mobile-drawer::before {
    content: '';
    position: absolute;
    top: 11%;
    left: -160%;
    width: 450%;
    height: 70%;
    background-image: url('/box animation slow1.webp');
    background-size: 100% 100%;
    background-repeat: no-repeat;
    z-index: 0;
    pointer-events: none;
    transform: rotate(90deg);
    transform-origin: center center;
    opacity: 0;
    transition: opacity 0.2s ease;
  }

  .mobile-drawer.is-open::before {
    opacity: 1;
    transition: opacity 0.2s ease 0.05s;
  }

  .mobile-drawer.is-open {
    transform: translateX(0);
  }

  .drawer-backdrop {
  display: none;
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.45);
  z-index: 140;
  opacity: 0;
  pointer-events: none;     /* ← add this line */
  transition: opacity 0.35s ease;
  backdrop-filter: blur(2px);
}

.drawer-backdrop.is-open {
  opacity: 1;
  pointer-events: auto;     /* ← add this line */
}

  .drawer-content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    height: 100%;
    gap: 0;
    /* Start invisible, fade in slightly after the ::before bg */
    opacity: 0;
    transition: opacity 0.15s ease;
  }

  .mobile-drawer.is-open .drawer-content {
    opacity: 1;
    transition: opacity 0.15s ease 0.2s;
  }

  .drawer-nav-links {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1.8rem;
    flex: 1;
    padding-top: 1rem;
    padding-left: 2rem;
  }

  .drawer-nav-links a {
    display: flex;
    text-decoration: none;
    color: inherit;
    width: 100%;
    justify-content: center;
    will-change: auto;
  }

  .drawer-nav-links .nav-button-wrapper {
    height: 90px;
    min-width: 0;
    width: 100%;
    max-width: 310px;
    animation: none;
  }

  .drawer-nav-links .nav-button {
    display: none;
  }

  .drawer-nav-links .nav-gif {
    display: block;
    max-height: 90px;
  }

  .drawer-nav-links .nav-button-text {
    position: absolute;
    width: 100%;
    z-index: 10;
    font-family: "Overload Burn", sans-serif;
    font-weight: 900;
    font-size: 1.4rem;
    color: #fff;
    pointer-events: none;
    text-align: center;
    text-shadow: 0 3px 8px rgba(0,0,0,0.6);
  }

  .drawer-social-section {
    border-top: 1px solid rgba(0, 153, 204, 0.3);
    padding-top: 1.25rem;
    margin-top: 0.5rem;
    
    
  }

  .drawer-social-title {
    font-family: "Superglue", sans-serif;
    font-size: 0.85rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #0e3947;
    text-align: center;
    margin-bottom: 1rem;
    padding-left: 2rem;
  }

  .drawer-social {
    display: grid;
    grid-template-columns: repeat(2, 68px);
    gap: 1rem;
    justify-content: center;
    align-items: center;
    margin-left: 2.5rem;
  }

  .drawer-social a:nth-child(5) {
    grid-column: 1 / 3;
    justify-self: center;
  }

  .drawer-social a {
    display: inline-flex;
    flex-shrink: 0;
    justify-content: center;
  }

  .drawer-social .gif-wrapper {
    width: 68px;
    height: 62px;
  }
}
</style>

<header transition:persist>
  <nav>
    <div class="nav-inner">

      <!-- Desktop nav links -->
      <div class="nav-links">
        <a href="/">
          <div class="nav-button-wrapper">
            <video src="/button home fast.webm" class="nav-button" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <video src="/button home slow.webm" class="nav-gif" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <span class="nav-button-text">Home</span>
          </div>
        </a>
        <a href="/portfolio">
          <div class="nav-button-wrapper">
            <video src="/button portfolio fast.webm" class="nav-button" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <video src="/button portfolio slow.webm" class="nav-gif" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <span class="nav-button-text">Portfolio</span>
          </div>
        </a>
        <a href="/about">
          <div class="nav-button-wrapper">
            <video src="/button about fast.webm" class="nav-button" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <video src="/button about slow.webm" class="nav-gif" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <span class="nav-button-text">About</span>
          </div>
        </a>
        <a href="/contact">
          <div class="nav-button-wrapper">
            <video src="/button contact fast.webm" class="nav-button" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <video src="/button contact slow.webm" class="nav-gif" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <span class="nav-button-text">Contact</span>
          </div>
        </a>
      </div>

      <!-- Desktop social links -->
      <div class="social-links">
        <a href="https://www.instagram.com/art.jacobg/" target="_blank" title="Instagram">
          <div class="gif-wrapper">
            <video src="/insta button slow.webm" class="gif base" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <video src="/insta button fast.webm" class="gif hover" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
          </div>
        </a>
        <a href="https://www.linkedin.com/in/jacob-gordillo-708021292" target="_blank" title="LinkedIn">
          <div class="gif-wrapper">
            <video src="/linked in button slow.webm" class="gif base" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <video src="/linked in button fast.webm" class="gif hover" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
          </div>
        </a>
        <a href="https://www.tiktok.com/@art.jacobg" target="_blank" title="TikTok">
          <div class="gif-wrapper">
            <video src="/tiktok button slow.webm" class="gif base" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <video src="/tiktok button fast.webm" class="gif hover" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
          </div>
        </a>
        <a href="https://x.com/artjacobg" target="_blank" title="X">
          <div class="gif-wrapper">
            <video src="/twitter button slow.webm" class="gif base" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <video src="/twitter button fast.webm" class="gif hover" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
          </div>
        </a>
        <a href="https://www.youtube.com/channel/UC5UHX0mjIxBgtZQJczX1lYA" target="_blank" title="YouTube">
          <div class="gif-wrapper">
            <video src="/youtube button slow.webm" class="gif base" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <video src="/youtube button fast.webm" class="gif hover" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
          </div>
        </a>
      </div>

      <!-- Mobile brand + hamburger -->
      <span class="nav-brand">Art-JacobG.com</span>
      <button class="hamburger" id="hamburger-btn" aria-label="Open menu" aria-expanded="false">
        <span></span>
        <span></span>
        <span></span>
      </button>

    </div>
  </nav>

  <!-- Backdrop -->
  <div class="drawer-backdrop" id="drawer-backdrop"></div>

  <!-- Mobile drawer -->
  <div class="mobile-drawer" id="mobile-drawer" aria-hidden="true">

    <!-- Stretched box bg -->

    <div class="drawer-content">

      <div class="drawer-nav-links">
        <a href="/">
          <div class="nav-button-wrapper">
            <video src="/button home fast.webm" class="nav-button" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <video src="/button home slow.webm" class="nav-gif" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <span class="nav-button-text">Home</span>
          </div>
        </a>
        <a href="/portfolio">
          <div class="nav-button-wrapper">
            <video src="/button portfolio fast.webm" class="nav-button" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <video src="/button portfolio slow.webm" class="nav-gif" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <span class="nav-button-text">Portfolio</span>
          </div>
        </a>
        <a href="/about">
          <div class="nav-button-wrapper">
            <video src="/button about fast.webm" class="nav-button" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <video src="/button about slow.webm" class="nav-gif" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <span class="nav-button-text">About</span>
          </div>
        </a>
        <a href="/contact">
          <div class="nav-button-wrapper">
            <video src="/button contact fast.webm" class="nav-button" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <video src="/button contact slow.webm" class="nav-gif" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            <span class="nav-button-text">Contact</span>
          </div>
        </a>
      </div>

      <!-- Social at bottom -->
      <div class="drawer-social-section">
        <p class="drawer-social-title">Social Media</p>
        <div class="drawer-social">
          <a href="https://www.instagram.com/art.jacobg/" target="_blank" title="Instagram">
            <div class="gif-wrapper">
              <video src="/insta button slow.webm" class="gif base" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
              <video src="/insta button fast.webm" class="gif hover" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            </div>
          </a>
          <a href="https://www.linkedin.com/in/jacob-gordillo-708021292" target="_blank" title="LinkedIn">
            <div class="gif-wrapper">
              <video src="/linked in button slow.webm" class="gif base" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
              <video src="/linked in button fast.webm" class="gif hover" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            </div>
          </a>
          <a href="https://www.tiktok.com/@art.jacobg" target="_blank" title="TikTok">
            <div class="gif-wrapper">
              <video src="/tiktok button slow.webm" class="gif base" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
              <video src="/tiktok button fast.webm" class="gif hover" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            </div>
          </a>
          <a href="https://x.com/artjacobg" target="_blank" title="X">
            <div class="gif-wrapper">
              <video src="/twitter button slow.webm" class="gif base" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
              <video src="/twitter button fast.webm" class="gif hover" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            </div>
          </a>
          <a href="https://www.youtube.com/channel/UC5UHX0mjIxBgtZQJczX1lYA" target="_blank" title="YouTube">
            <div class="gif-wrapper">
              <video src="/youtube button slow.webm" class="gif base" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
              <video src="/youtube button fast.webm" class="gif hover" muted autoplay playsinline loop preload="metadata" aria-hidden="true"></video>
            </div>
          </a>
        </div>
      </div>

    </div><!-- end drawer-content -->
  </div><!-- end mobile-drawer -->

  <script is:inline>
    (function () {
      const pageOrder = {
        '/': 0,
        '/portfolio': 1,
        '/about': 2,
        '/contact': 3,
      };

      const root = document.documentElement;

      const normalizePath = (href) => {
        const url = new URL(href, location.origin);
        const pathname = url.pathname.replace(/\/$/, '');
        return pathname === '' ? '/' : pathname;
      };

      const getDirection = (href) => {
        const current = normalizePath(location.pathname);
        const target = normalizePath(href);
        const currentIndex = pageOrder[current] ?? 0;
        const targetIndex = pageOrder[target] ?? 0;
        return targetIndex >= currentIndex ? 'forward' : 'backward';
      };

      document.querySelectorAll('.nav-links a[href], .drawer-nav-links a[href]').forEach((anchor) => {
        anchor.addEventListener('click', (event) => {
          if (anchor.target === '_blank' || event.metaKey || event.ctrlKey || event.shiftKey || event.altKey) return;
          const href = anchor.getAttribute('href');
          if (!href || normalizePath(href) === normalizePath(location.pathname)) return;
          event.preventDefault();

          root.dataset.pageTransition = getDirection(href);
          const navigate = () => window.location.assign(href);

          if (document.startViewTransition) {
            document.startViewTransition(navigate);
          } else {
            navigate();
          }
        });
      });

      // ── Drawer logic ──
      const btn = document.getElementById('hamburger-btn');
      const drawer = document.getElementById('mobile-drawer');
      const backdrop = document.getElementById('drawer-backdrop');

      function openDrawer() {
        btn.classList.add('is-open');
        drawer.classList.add('is-open');
        backdrop.classList.add('is-open');
        btn.setAttribute('aria-expanded', 'true');
        drawer.setAttribute('aria-hidden', 'false');
        document.body.style.overflow = 'hidden';
      }

      function closeDrawer() {
        btn.classList.remove('is-open');
        drawer.classList.remove('is-open');
        backdrop.classList.remove('is-open');
        btn.setAttribute('aria-expanded', 'false');
        drawer.setAttribute('aria-hidden', 'true');
        document.body.style.overflow = '';
      }

      btn.addEventListener('click', () => {
        btn.classList.contains('is-open') ? closeDrawer() : openDrawer();
      });

      backdrop.addEventListener('click', closeDrawer);

      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape') closeDrawer();
      });

      drawer.querySelectorAll('a').forEach((link) => {
        link.addEventListener('click', closeDrawer);
      });
    })();
  </script>
</header>